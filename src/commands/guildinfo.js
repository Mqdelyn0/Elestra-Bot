const Discord = require('discord.js');

module.exports = {
    commands: ['guildinfo'],
    expected_args: '',
    min_args: 0,
    max_args: 0,
    permissions: [],
    required_roles: [],
    callback(client, message, arguments, raw_text) {
        
    }
}